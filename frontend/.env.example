# DockOps Frontend — Environment Configuration
# Copy to .env and fill in values

VITE_API_URL=http://localhost:8000

# Optional: API key for write operations (must match backend's API_KEY env var)
VITE_API_KEY=
